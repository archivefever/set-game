<h1 id="page-header">SET</h1>
<span id="response-bar"></span><br>
<span id="remaining-cards"></span>
<div id="card-grid">

  <ul id="all-cards">
    <% @game.initial_deal.each do |card| %>
      <%= render partial: "partials/card_show", locals: {card: card } %>
    <% end %>
  </ul>

</div>
<span id="game-id"><%=@game.id %></span>